{
  "name": "wbs-json-spec",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node --experimental-strip-types tools/validate.ts",
    "validate:all": "node --experimental-strip-types tools/validate-all.ts",
    "normalize": "node --experimental-strip-types tools/normalize.ts",
    "apply": "node --experimental-strip-types tools/apply.ts",
    "test": "node --experimental-strip-types --test tests/apply/*.test.ts"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
